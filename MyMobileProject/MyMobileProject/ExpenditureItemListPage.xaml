<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyMobileProject.ExpenditureItemListPage" BackgroundColor="LavenderBlush">


    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+" Clicked="OnItemAdded">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="Android, UWP" Value="plus.png" />
                </OnPlatform>
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <StackLayout Spacing="5" Padding="20" BackgroundColor="LightSlateGray">
            <Label Text=" We do not provide total amount, it only shows what you spend, use calculator!"
                   
                   VerticalTextAlignment="Center"  HorizontalOptions="StartAndExpand" TextColor="Black" />

            <Label Text="       Category                                                                               Value"
                   VerticalTextAlignment="Center"  HorizontalOptions="StartAndExpand" TextColor="White" />

            <ListView x:Name="listView" Margin="20"  ItemSelected="OnListItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>

                        <ViewCell>
                            <Frame Margin="5,5,5,3" Padding="2,2,2,2" CornerRadius="0" OutlineColor="LightGray" HasShadow="False"
                           HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">

                                <StackLayout Orientation="Horizontal" Padding="0,0,0,0" Margin="0,0,0,0" HorizontalOptions="FillAndExpand">
                                    <BoxView WidthRequest="3" BackgroundColor="OrangeRed" Margin="0,0,0,0" />

                                    <Label  Grid.Row="0" Grid.Column="0" 
                                             FontSize="15" FontAttributes="None" TextColor="White" 
                                            Text="{Binding Name}" VerticalTextAlignment="Center"
                                            HorizontalOptions="StartAndExpand" />

                                    <Label Grid.Row="1" Grid.Column="2"  Margin="0,0,10,0"
                                           FontSize="18" FontAttributes="Bold" TextColor="White" 
                                           x:Name="getNums" Text="{Binding Notes}"
                                           VerticalTextAlignment="End"  HorizontalOptions="EndAndExpand" />

                                    <Label x:Name="TotalAmount " Text="{Binding AnswerValue}" HorizontalOptions="Center"
                                           VerticalTextAlignment="Center" />

                                    <Image Source="check.png" HorizontalOptions="End" IsVisible="{Binding Done}" />

                                </StackLayout>

                            </Frame>
                        
                        </ViewCell>
                        
                    </DataTemplate>
                    
                </ListView.ItemTemplate>

            </ListView>
            <Button x:Name="addExpenditure" Text="+ expenditure"  TextColor="WhiteSmoke" FontSize="Small" HorizontalOptions="CenterAndExpand"  
                    BackgroundColor="Gray" Clicked="AddExpenditure_Clicked"/>

            

        </StackLayout>
    </ScrollView>
</ContentPage>
